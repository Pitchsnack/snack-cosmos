/**
 * Placeholder — Intake Queue reassignment dialogs will land with the
 * Queue backend adapter (PRD §15). Kept as thin no-op wrappers so any
 * lingering import compiles cleanly; the canonical Queue page renders a
 * controlled unavailability state instead.
 */
export function ReassignDialog() {
  return null;
}
